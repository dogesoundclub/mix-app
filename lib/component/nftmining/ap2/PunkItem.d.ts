import { DomNode } from "skydapp-browser";
import AnimalsPunksV2Tab from "./AnimalsPunksV2Tab";
export default class PunkItem extends DomNode {
    private tab;
    private id;
    private punk;
    private mixAmount;
    private claimable;
    private refreshInterval;
    constructor(tab: AnimalsPunksV2Tab, id: number);
    private loadImage;
    private load;
    delete(): void;
}
//# sourceMappingURL=PunkItem.d.ts.map